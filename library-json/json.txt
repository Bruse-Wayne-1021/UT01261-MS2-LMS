 re install json server 
npm install -g json-server
cd path/to/your/project
If you want to avoid changing PowerShell’s execution policy, you can run the JSON server in the Command Prompt instead of PowerShell:

Press Windows + R, type cmd, and hit Enter.
Navigate to the folder where your db.json file is located.
Run the command:
cmd
Copy code
json-server --watch db.json

npm start



install pacage.json
npm init -y

angular
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
